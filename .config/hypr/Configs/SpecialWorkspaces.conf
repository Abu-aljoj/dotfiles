### Special Workspaces ###

#Btop
bind = ALT, B, togglespecialworkspace, btop

exec-once = [workspace special:btop silent] kitty --class=btopterm -e btop

windowrule = workspace special:btop, class:^(btopterm)$
windowrule = float, class:^(btopterm)$
windowrule = size 60% 60%, class:^(btopterm)$
windowrule = center, class:^(btopterm)$

#Youtube Music
#bind = ALT, Y, togglespecialworkspace, youtubemusic

#exec-once = [workspace special:youtubemusic silent] youtube-music

#windowrule = workspace special:youtubemusic, title:^(YouTube Music)$
#windowrule = float, title:^(YouTube Music)$
#windowrule = size 60% 60%, title:^(YouTube Music)$
#windowrule = center, title:^(YouTube Music)$


# Minimizing Applications
#bind = ALT, Q, togglespecialworkspace, minimize
#bind = SHIFT, Tab, movetoworkspacesilent, special:minimize
